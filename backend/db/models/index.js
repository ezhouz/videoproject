const uploaderInfo = require('./uploaderInfo');
const voteCount = require('./voteCount');
const voteLog = require('./voteLog');

module.export = {
    user: uploaderInfo,
    video: voteCount,
    voteLog: voteLog
}
